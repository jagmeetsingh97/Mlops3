Purpose: Model training using scikit-learn.

Action:

Trains a LinearRegression model on the California Housing dataset.

Saves the model as sklearn_model.joblib in the models/ directory.

Key File: train.py